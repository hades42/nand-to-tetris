#include "code_writer.h"

int code_writer_open(CodeWriter *code_writer, const char *filename) {
    // change file extension 
    char *new_file[1024];
    int read = 0;
    int write = 0;
    while(filename[read] != '\0' && filename[read] != )
}